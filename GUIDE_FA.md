# 🔌 راهنمای قدم‌به‌قدم اتصال به Instagram Graph API

این راهنما برای فعال‌سازی **انتشار خودکار پست و استوری** و **آنالیز خودکار پیج** است.
اگر این مراحل را انجام ندهید، سیستم همچنان همه محتوا را می‌سازد و زمان‌بندی می‌کند؛ فقط انتشار را خودتان دستی انجام می‌دهید (دکمه «⬇️ بسته»).

---

## قدم ۱ — تبدیل اکانت اینستاگرام به حرفه‌ای (Business/Creator)

1. اپلیکیشن اینستاگرام را باز کن.
2. **تنظیمات ← نوع حساب و ابزارها ← تغییر به حساب حرفه‌ای** (Settings → Account type → Switch to Professional).
3. دسته‌بندی (مثلاً «فروشگاه») را انتخاب کن و ادامه بده.

> ⚠️ اکانت شخصی (Personal) امکان انتشار از طریق API را ندارد.

## قدم ۲ — اتصال به یک فیسبوک‌پیج

1. در اینستاگرام: **تنظیمات ← حساب ← صفحه‌های فیسبوک متصل**.
2. یک پیج انتخاب کن (یا اگر نداری، یک پیج ساده برای فروشگاهت بساز).
3. مطمئن شو اکانت فیسبوکت ادمین آن پیج است.

## قدم ۳ — ساخت اپ در متا دیولوپرز

1. وارد `https://developers.facebook.com` شو.
2. **My Apps ← Create App**.
3. نوع اپ را **Business** انتخاب کن.
4. نام دلخواه بده (مثلاً «مدیریت پیج من»).
5. در داشبورد اپ: **Add Product ← Instagram** را اضافه کن (همراه با Facebook Login).
6. در **App Settings ← Basic** آدرس سایتت را وارد کن.

## قدم ۴ — گرفتن Access Token

1. به `https://developers.facebook.com/tools/explorer` برو.
2. از منوی بالای سمت راست، **اپ خودت** را انتخاب کن.
3. از منوی کنار توکن («User or Page»)، اگر گزینه **Instagram Graph API** هست همان را انتخاب کن؛ وگرنه پیج فیسبوک متصل را انتخاب کن.
4. در بخش **Permissions** این پرمیشن‌ها را اضافه کن:
   - `instagram_basic`
   - `instagram_content_publish`
   - `instagram_manage_insights`
   - `pages_show_list`
   - `pages_read_engagement`
5. دکمه **Generate Access Token** را بزن و توکن را کپی کن.

## قدم ۵ — پیدا کردن شناسه اکانت اینستاگرام (IG User ID)

1. توکن را در پنل (تب تنظیمات ← اتصال اینستاگرام) بچسبان.
2. دکمه **«🔍 پیدا کردن خودکار اکانت»** را بزن — سیستم شناسه `1784...` را پیدا می‌کند.
3. دکمه **«🧪 تست و ذخیره اتصال»** را بزن. اگر پیغام موفقیت دیدی، تمام شد! 🎉

## قدم ۶ (اختیاری) — توکن بلندمدت ۶۰ روزه

توکن Graph Explorer کوتاه‌مدت است. برای توکن ۶۰ روزه:

1. در تنظیمات اپ متا، **App Mode** را روی **Live** ببر (برای اپ شخصی، حالت Development هم کار می‌کند اما توکن همچنان کوتاه است).
2. از این URL استفاده کن (مقادیر را جایگزین کن):
   ```
   https://graph.facebook.com/v20.0/oauth/access_token?grant_type=fb_exchange_token&client_id=APP_ID&client_secret=APP_SECRET&fb_exchange_token=SHORT_TOKEN
   ```
3. توکن بلند جدید را در پنل ذخیره کن.
4. یادآور شو هر ۲ ماه یک‌بار توکن را تازه کنی (در داشبورد پنل هشدار می‌گیریم).

## بررسی وضعیت انتشار خودکار

- دکمه **«🌐 بررسی دسترسی عمومی سرور»** را بزن.
- اگر «قابل دسترسی» بود، انتشار خودکار فعال است.
- اگر نه: اپ را روی یک سرور عمومی (VPS، Railway، Render و…) اجرا کن یا از حالت دستی استفاده کن.

## محدودیت‌های API اینستاگرام (مهم)

| مورد | محدودیت |
|------|---------|
| انتشار پست | حداکثر ۲۵ پست در ۲۴ ساعت |
| استوری | انتشار فقط تصویر/ویدیو — بدون استیکر، لینک و موزیک |
| اینسایت | نیازمند پرمیشن `instagram_manage_insights` |
| اکانت | فقط اکانت‌های Business و Creator |

## اگر به متا دسترسی نداری (تحریم/فیلتر)

نگران نباش — سیستم در حالت **دستی** کاملاً کار می‌کند:
- همه پست‌ها و استوری‌ها ساخته و زمان‌بندی می‌شوند
- در زمان مقرر در تب «تقویم انتشار» به حالت **«آماده انتشار دستی»** درمی‌آیند
- با دکمه **«⬇️ بسته»** تصویر + فایل کپشن را دانلود می‌کنی و در ۳۰ ثانیه در اپ اینستاگرام منتشر می‌کنی
- آمار را می‌توانی روزانه در تب آنالیز **دستی ثبت** کنی تا نمودار رشد داشته باشی
